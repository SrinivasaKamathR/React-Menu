import Logo from "../assets/food.png";

const Title = () => {
  return (
    <a href="/">
      <img className="logo" src={Logo} alt="image not Found" />
    </a>
  );
};

export default Title;
